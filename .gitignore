# dependencies
node_modules/

# local env with real secrets — never commit; use `wrangler secret put`
.env
.env.local

# cloudflare / wrangler
.wrangler/
.dev.vars

# tooling / os
.DS_Store
.playwright-mcp/
.impeccable/
