# dependencies
node_modules/

# local env with real secrets — never commit; use `wrangler secret put`
.env
.env.local

# cloudflare / wrangler
.wrangler/
.dev.vars

# tooling / os
.DS_Store
.playwright-mcp/
.impeccable/

# Edit Images dev tool: retained originals + drop staging (local only)
_asset_originals/
assets/images/incoming/

# guest privacy: plaintext list + token register never enter git
src/*.private.json
src/*.private.csv
src/*.private.txt
src/*.private.tsv

# internal governance docs (contain budget/vendor figures) — never in the public repo
docs/DECISION-REGISTER.md
docs/PUBLICATION-DATA-MATRIX.md
docs/RELEASE-GATES.md
docs/REVIEW-VIENTIANE-PASS.md

# local dev tooling (launch config)
.claude/

# Window 007 QA evidence (local only — screenshots and PDFs of synthetic guests)
docs/acceptance/2026-09-24-window-007/visual/
docs/acceptance/2026-09-24-window-007/out/*.png
docs/acceptance/2026-09-24-window-007/out/*.pdf

# the layout QA agent's diagnostic artifacts (reports, annotated screenshots) — local QA evidence, never committed
qa-artifacts/
