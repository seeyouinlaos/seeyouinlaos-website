# Release 014 · the plan Codex was asked to review (pre-implementation, 19 Sep 2026 15:15 — refused by quota)

## The Owner's instruction, reduced to decisions
1. **Delete the fixed-arrangement concept** everywhere (engine, Worker, drafts, mail, client, pages, tests). The hosts start at zero; nothing is held for anyone in advance; no "Arranged for you".
2. **Canonical model**: participation (scope) → the ten stages A–J → package mode (Complete / Essential / Individual) → per stage confirmed · waitlisted · declined · open → readiness → Bag → cost → Review → Profile → confirmation.
3. **Packages are configuration** (`assets/packages-data.js`): per covered stage the default product and the ordered fallback chain (default → the same house's neighbours from the more affordable side of the default outwards → the alternative houses). Capacity decides, never price. Complete = all ten stages; Essential = the Owner's booking model of 20 Sep 2026: stage D alone (the Wedding Stay 27 Feb – 01 Mar), D1 preselected — the Souphattra Heritage, the Heritage Executive — then the house's categories in the hotel's order, then the waiting list (never a silent move to D2 / D3); D2 the Guest House complimentary and D3 the Riverside Hotel are the guest's own alternatives of the same stage; stage C is never auto-added; the amount is the chosen room's (the Executive USD 155). The Overview (002) defines stages and alternatives (A1 – A3, D1 – D3); the Accommodation Details (003) are the product record — its "C + D1" label is applicability, never a composition (the morning's C + D1 reading is withdrawn; `test/booking-model.test.mjs` pins the model). The return flight is the Owner's MU5922 + MU741 (06.03.2027 · 10:00 → 14:55 via Kunming, USD 200 unchanged); no earlier number remains on any surface.
4. **Party capacity**: a unit must take the whole party (`need` = the party's members; a party member already in the unit counts). No partial booking of a couple. Client (`fitsParty`, `unitForParty`, `canTake`) and engine (`join … need`, 409 `full for your party`) agree.
5. **Waiting list LAST**: when no option of a chain can take the party, the stage is waitlisted in the engine (`wl:<stage>|<guestId>`, deterministic positions by time, renumbered on every read, cleared by a hold, a leave, an assignment and the clean reset); USD 0 until resolved; an answered stage for readiness; shown on My Trip, Profile, Review and in both emails; the package is never called "fully confirmed".
6. **A package REPLACES a conflicting manual selection on confirmation**, previewed first, bound to the preview by a plan signature (the confirm re-reads the engine; a changed plan is redrawn, nothing applied). A held room is never released before its replacement is held (the engine holds first, then lets the old place go).
7. **D2 = Guest House complimentary**: engine key `guesthouse/guest-house`, ONE unit of SIX places, complimentary, occupants' first names visible to signed-in guests (never to the public), never "Private Residence", never "up to 4"; the room page shows the live places.
8. **Canonical counts**: relevant = confirmed + waitlisted + declined + open; excluded = outside the scope; bagItems = actual lines; bagTotal = chargeable confirmed lines.
9. **The current master wins**: C86 USD 105; Lijiang and Kempinski six rooms per category; the dated venues (21.02 Sühring dinner; the Aman tea 24.02; 23.02 Baan Phraya; 07.03 Cannubi + Harudot; 08.03 Petits Plats; Thong Smith undated; The Commons the mall); Baan Phraya / Cannubi / Riverside media from the Owner's Drive folders with provenance; no dish close-ups.
10. **Register**: the current 007 rows; placeholder rule ("Aob's girlfriend", "." surname); stable codes; nothing printed.
11. **Final clean reset LAST**, three-mode flow; test data never on live; READY TO SEND gated by Codex.

## Questions for the reviewer (pre)
- Is `need` handled identically by the planner (`unitForParty`) and the engine (`join`), including a party member already in the unit? Any path where a couple is split?
- Can a waitlisted guest hold a place in the same stage (409)? Does a hold from any path (join, package confirm, GR assign) clear the entry? Does the reset clear `wl:`?
- Is the preview signature complete (stage · why · product · unit · amount)? Can a plan change between preview and apply escape it?
- Does the package confirm ever release a room before its replacement is held?
- Do counts hold their invariant for every scope × state combination, with complimentary and waitlisted stages at 0?
- Is host-ness only ever the authenticated flag (record.hosts), never a client field?

## Final review (post-deploy, owed)
Scope: `git diff f88b9f6..main` (release 014) on the deployed build; the questions above; the read-only live acceptance results; the reset execution record. Classify every finding, fix P0/P1, confirming pass until no [high] remains, then prove parity.
Added 20 Sep 2026 (still owed to Codex — quota until 24 Sep 22:19 CEST; never claimed passed): the Owner's Essential trip (stage D alone, D1 preselected, the Souphattra-only fallback, the waiting list, D2 / D3 the guest's) and the return flight MU5922 + MU741 — the E2E in `docs/acceptance/2026-09-19-release-014/e2e.mjs` §6 and §6b are the record to review against.
Added 20 Sep 2026 (the guest-list go-live — still owed to Codex, quota until 24 Sep 22:19 CEST; never claimed passed): the register rebuilt from the current 007_Guestlist (104 active · 81 parties · 84 codes preserved · 20 new · 6 cancelled; person-keyed aliases, CONxxx per guest, duplicate rows reported), the person id and couple state in the served index (`c` · `k`) stamped on every stored journey by the Worker (`personOf`, beside the frozen auth authority), the personal details (birth date · nationality · phone · email · mailing address) on `/api/contact`, the invitation page and the profile with the sheet's data prefilled once per signed-in person. Review lenses: authentication isolation, code collisions, identity and couple mapping, duplicate humans, private-code and personal-data leakage, couple-profile cross-over, stale state, cross-session leakage, package regression — `docs/acceptance/2026-09-20-guestlist-go-live/README.md`, `test/personal-details.test.mjs`, E2E 014 §6c.
Added 20 Sep 2026 (the Highlights — still owed to Codex, probed again: quota until 24 Sep 22:18 CEST; never claimed passed): the Highlights row and rail, the shared booking grammar `assets/highlight.js` (select → preview → confirm → one Bag line per house, put replaces), the Sühring menus USD 294/234, Baan Phraya 114, Cannubi 165 (pairings never products), the draft module on the experience/tea pages, the menu-aware re-pricing, the Sathorn inset fix (`.pcbody` + `--pc-inset`). Review lenses: duplicate Bag lines, booking before confirmation, stale draft resurrecting a removed line, cross-device consistency, price integrity, party/resource isolation (requests hold no inventory), regression of the package/waitlist flows — `docs/acceptance/2026-09-20-highlights/`, `test/highlights.test.mjs`.
Added 20 Sep 2026 (the final booking-UX pass — still owed to Codex, probed again: quota until 24 Sep 22:19 CEST; never claimed passed): the booking context `assets/wizard.js` (a Journey page opened from 02 / 06 is a sub-view with the step shell, RETURN TO MY TRIP, an automatic return to the stage it came from after a selection; `?ctx=trip&stage=<key>` explicit in the URL, never a store; the global Journey untouched), the seating ledger's `holder` for authenticated reads and the portrait-by-id route (`GET /api/profile/photo?of=<guest id>`, bearer required, read only), the roll call beside the plan (`assets/seating.js` railHtml / wireRail, `assets/avatar.js` of), CONFIRM SEAT as the primary action with Continue stepped back, the photography words + acknowledgement, Coffee & Cake on My Profile, the photo CTA, Save my progress in the account block, the reference galleries `assets/refgal.js`, the Highlight dress codes, the ticket padding at `.p-sum-ticket`, the Snow Mountain frame labelled as the view. Questions for Codex: can a sub-view ever reach the Bag or the engine through a path that My Trip does not (no — the same `ST.select` / `SIYL_BAG.put`; verify); can the `of` read leak a photo to an unauthenticated caller or to a write path (401 / own key only; verify); can the rail name a guest in a public view (`named:false` → no rail; verify); does browser Back after a room change ever resurrect the earlier room (engine + Bag replace by id; verify). Tests: `test/final-pass.test.mjs`, `docs/acceptance/2026-09-20-final-pass/e2e.mjs`.
