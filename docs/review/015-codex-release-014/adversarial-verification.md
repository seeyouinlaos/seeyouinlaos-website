# Adversarial verification · release 014 (substitute evidence — NOT a Codex review)

Six independent Claude subagents read the working tree (`git diff f88b9f6` + untracked) against the Owner's model, each through one lens (engine · planner/confirm · counts/readiness · D2/pages/privacy · data reconciliation · worker/mail/reset); every finding was then handed to two refuters. The refute stage was cut short by the subagent session limit (36 of 116 verdicts returned; 29 refuted, 7 confirmed-or-not-refuted). Every finding was afterwards read and decided by hand; the fixes are in the release commit.

| Sev | Where | Finding (short) | Decision |
|---|---|---|---|
| P1 | `assets/stay.js:116` | "Not joining this stage" / Remove on a held stay before the engine has been read (or while it is unreachable) drops the Bag line without releasing the… | FIXED — `ST.remove` reads the engine first; when it cannot be read the release is still asked of the engine (refused as unreachable, the line stays) |
| P1 | `journeys.html:362` | "I am not joining this part of the journey" on the journeys page is a bare skip toggle: for a waitlisted stage it marks the stage declined but leaves … | FIXED — The Journey's Not joining goes through `J.decline()` (release, unwait, then decline) and shows the waiting-list state |
| P1 | `assets/guest.js:449` | A scope change that excludes a waitlisted stage never leaves the waiting list: staleFor() and reconcileScope() handle Bag lines, engine holds, seats a… | FIXED — `staleFor` names it, `reconcileScope` gives the waiting-list place back |
| P1 | `assets/journey.js:270` | packagePlan() does not recognise a guest who already holds the package's default category in a unit other than the first free one: unitForParty() retu… | FIXED — a place the guest holds in the very category is theirs (`mineFor` first), never moved |
| P1 | `src/worker.js:407` | The submission fingerprint includes the guest's waiting-list POSITION, so every time someone ahead of them leaves the line or is assigned a room, a gu… | FIXED — the fingerprint carries the stage and the size, never the position |
| P1 | `src/rooms.js:193` | The engine stores the occupant `name` verbatim (only `.slice(0, 24)`, never `displayName()` from src/auth.js, which nothing imports) and shows it to e… | FIXED — `displayName()` on every join/wait; the Journey page escapes the words |
| P1 | `src/rooms.js:227` | `need` is a point-in-time check only: a party member's join verifies the unit has `need` free places at that instant, but nothing keeps the places for… | FIXED — PARTY PLACES: the places the absent members will take are kept in the unit (and, for a party larger than a unit, across the category); a stranger cannot take them; they go with the party |
| P1 | `room.html:315` | The guest-house room page never recognises the guest's own Guest House line: `mine(w)` matches a Bag line by `line.meta.indexOf(room.name)`, but the c… | FIXED — the line is recognised by its slug |
| P1 | `.assetsignore:32` | The working checkout's root holds `invitation-code-list-final.csv` and `.txt` (columns guestId, firstname, surname, invitationId, invitationCode — 47 … | REPORTED to the Owner — `.assetsignore` is an infra-pinned file (freeze): the two local code lists should be moved out of the checkout or excluded with an OWNER-INFRA-CHANGE commit; the release path (Workers Build from git) never ships them |
| P1 | `docs/acceptance/2026-09-19-release-014/live-ro.mjs:35` | The new read-only live acceptance script uses a REAL register surname (borne by two guests in src/guestlist.private.json) as a leak-canary inside the … | FIXED before any commit — no name in the script |
| P1 | `your-journey.html:484` | fxConfirm never re-checks that the drawer is still open after its engine re-read: Cancel (or the shell's Escape/scrim/Close, which do not even reset F… | FIXED — the confirm applies only while the drawer the guest confirmed is still open (`FX_OPEN`/`FXMODE`/`#fxg`), the shell announces every close (`siyl:drawer-close`), a failed re-read never applies |
| P1 | `assets/rooms.js:89` | unitForParty returns the FIRST fitting unit, not the guest's own, so a guest who already holds the package's default category in any room other than t… | FIXED — same as above |
| P1 | `your-journey.html:500` | fxConfirm discards the results of ST.remove (line 495) and U.wait (line 500) and asserts FX_DONE.waitlisted = plan.waitlist unconditionally (line 504)… | FIXED — the engine's answers decide what the summary says; a failed release never leads to a wait; a lifted decline is put back when the hold fails |
| P1 | `assets/rooms.js:83` | The one-unit rule (fitsParty / engine need) makes every hotel stage unbookable for any party of three or more — by package AND by hand — even on an EM… | FIXED — a party larger than a unit fills one unit and keeps the rest of its places across the category (engine + client) |
| P1 | `assets/aman.js:39` | The site-wide menu, the shared footer and the recon footer still label Sühring as a LUNCH: assets/aman.js:39 ['Sühring · Lunch in Bangkok', …], assets… | FIXED — Dinner |
| P2 | `your-journey.html:495` | fxConfirm ignores a failed release of a held room in a waitlist row and the engine's 409 on U.wait, then reports the stage as 'On the waiting list' al… | FIXED — same |
| P2 | `assets/journey.js:318` | When the engine is unread or unreachable on a device, a waitlisted stage reads 'open': readiness asks the guest to 'choose or say you are not joining'… | FIXED — the device remembers the line (`siyl.wait`, dropped by the reset) until the engine answers |
| P2 | `assets/journey.js:213` | state() lets an unheld Bag line hide a live waiting-list entry for the same stage: counts.waitlisted 0, readiness asks for a room, profile/review show… | ACCEPTED — the engine is the truth; a line without a hold is named by readiness ("choose your room") and `sync` drops leftovers; the entry stays in the engine |
| P2 | `src/mail-templates.js:78` | A waitlisted stage can be priced in the email and counted in the total: the Worker stores and mails the client's `selections`/`totalUsd` verbatim, so … | FIXED — a line for a stage the engine has waitlisted is dropped from the emails and the total recomputed |
| P2 | `src/rooms.js:214` | `wait` stores the client's `wanted` array with no per-string length bound, and the Worker forwards any body size to the single Rooms actor: an authent… | FIXED — 64 characters, only keys of the seed, 12 entries |
| P2 | `src/rooms.js:214` | `wanted` is length-capped to 12 entries but each entry is an unbounded string: an authenticated guest can store megabytes per (stage, guest) — over th… | FIXED — see above |
| P2 | `assets/journey.js:241` | `need` sent to the engine is always the register party size (`partySize()` = members.length), never the number of party members who actually join that… | ACCEPTED as the Owner's rule (the party books together); a member who is not joining a stage gives the kept place back through Not joining |
| P2 | `voyage.html:155` | Public served copy still describes the retired concept: 'A private residence in downtown Vientiane is offered as an alternative stay.' The brief says … | FIXED — voyage.html |
| P2 | `assets/pricing.js:388` | No migration retires the old Bag id `airbnb-2br`. A Bag saved by release 013 keeps its 'Private Residence · Vientiane · Complimentary · up to 4 guests… | FIXED — a complimentary line that is not the Guest House leaves the Bag on load |
| P2 | `review.html:292` | On Review & Send and in both emails the complimentary Guest House line is priced in words: review.html prints `J.quantityLine(x)` = 'USD 0 per person … | FIXED |
| P2 | `assets/journey.js:283` | A stage where the guest already HOLDS a room but no chain option can take the party gets why:'waitlist' with replaces = the held line; the confirm rel… | FIXED — the held room stays as chosen |
| P2 | `your-journey.html:482` | The confirm ignores the result of its own re-read: U.load(true) resolves null on failure but leaves the previous `view` in place, so plan.ready stays … | FIXED — a failed re-read never applies |
| P2 | `assets/journey.js:296` | planSignature covers stage·why·key·unit·amount but not `replaces` (or wasDeclined/need): a hold the same guest took meanwhile on another device is pul… | FIXED — replaces, wasDeclined and the party size are in the signature |
| P2 | `your-journey.html:489` | The 'not joining' of a covered stage is lifted (plan.unskip) BEFORE any hold is attempted; when the hold then fails the stage is left 'open', readines… | FIXED |
| P2 | `your-journey.html:397` | reconcileScope (and SIYL_GUEST.staleFor) release rooms, Bag lines and seats outside the new scope but never the waiting-list entry: a guest who leaves… | FIXED |
| P2 | `assets/journey.js:114` | decline() only unwaits when the engine view is already loaded (`U.ready()`); 'Not joining this stage' tapped while the view is null (initial render, s… | FIXED — the engine is asked whether read or not |
| P2 | `src/rooms.js:230` | `need` is checked only at the moment of one guest's join and reserves nothing for the rest of the party: after the first member joins a two-place room… | FIXED — PARTY PLACES (the places the absent members will take are kept in the unit and go with the party) |
| P2 | `assets/rooms.js:55` | load(true) supersedes `loading` but the superseded fetch still assigns `view = d` when it lands: the drawer's own read (fxOpen line 461) issued just b… | FIXED — only the latest read or write becomes the view |
| P2 | `assets/journey.js:49` | The Aman tea's new date (24 FEB) and its chronological position never reach the guest's actual bag line: tea.html:63 writes the line as id 'tea1872' (… | FIXED — `tea1872` carries the same place and date |
| P3 | `review.html:292` | The Guest House complimentary line on Review & Send reads 'USD 0 per person · your cost' and 'USD 0' in the amount column, while My Trip and My Bag sa… | FIXED — Complimentary on Review and in both emails |
| P3 | `assets/journey.js:314` | counts().bagTotal is B.total(), so it includes lines of excluded stages; the brief's 'bagTotal = chargeable confirmed lines only' does not hold while … | FIXED — the chargeable lines of the guest's own trip |
| P3 | `your-journey.html:340` | A package whose every planned row is a waitlist row (total 0) is priced 'Complimentary' on the card and in the preview total.… | FIXED — "No cost until the waiting list is resolved" |
| P3 | `your-journey.html:489` | fxSummary's 'Could not be held just now: … — as it was before' is untrue for a stage the guest had declined: plan.unskip runs before any hold, so a fa… | FIXED — the lifted decline is put back |
| P3 | `review.html:459` | buildText (the mailto backup channel and the text the Worker receives) lists NOT JOINING over all SEGMENTS without J.relevant — a declined stage of an… | FIXED — NOT JOINING filtered to the trip; a WAITING LIST section |
| P3 | `your-journey.html:519` | Dead listener from the retired planner: a second siyl:units handler redraws the preview only when FXMODE==='full', a mode that no longer exists.… | FIXED — removed |
| P3 | `src/worker.js:560` | The reset's post-sweep `remaining` summary omits the waiting list: it reports occupancies, seat holds and KV keys only, so the 'zero' proof the operat… | FIXED — `remaining.waitlisted` |
| P3 | `src/rooms.js:213` | A second `wait` for a stage the guest already waits for keeps the first entry untouched, including `size` and `wanted`; a party that grew (a member ad… | FIXED — a second wait keeps the place in the line and updates size and wanted |
| P3 | `src/rooms.js:214` | A repeated `wait` for a stage keeps the stored entry untouched (`if (!cur) put`), so `size` and `wanted` never update: the line GR reads in `plan`, th… | FIXED |
| P3 | `src/rooms.js:52` | `stageOf()` falls back to the window itself for a key no longer in STAGE_OF, and `occupancies()`/`view()` do not filter keys absent from SEED, so a ho… | FIXED — rows of a retired key are ignored by the engine; the production reset clears them anyway |
| P3 | `src/rooms.js:212` | `wait` refuses while a place is held (409), so the only way to move a held stage onto the waiting list is two non-atomic guest writes — `leave` then `… | MITIGATED — a held room is never traded for the waiting list any more (the plan keeps it) |
| P3 | `your-journey.html:146` | The room chooser under the Guest House line explains 'Every room has two places. Join a room where someone you know already is, or take an empty one.'… | FIXED |
| P3 | `assets/stay.js:225` | Refusal and disabled-CTA words call the six-place house a room: a seventh guest is told 'This room was just filled. Please choose another room.' and a… | FIXED |
| P3 | `assets/i18n/catalog-public.json:178` | Retired-label residue in served or gate files: assets/i18n/catalog-public.json (served from assets/, referenced by no page, dated 30 Aug) still lists … | FIXED — the stale dictionary entries removed; gate R3 pins the retired register only |
| P3 | `your-journey.html:428` | The preview names the Guest House unit 'Room A' (`'Room '+r.unit` fallback) and its category as 'six shared places' (meta split), although U.unitName … | FIXED |
| P3 | `your-journey.html:443` | A package applied when everything is already held reports 'Essential trip selected 0 stages for you.' and the package card still prices the whole pack… | FIXED — "changed nothing — every stage it covers was already yours" |
| P3 | `your-journey.html:519` | A second siyl:units listener still tests `FXMODE==='full'` — the deleted planner mode's name — and can never fire; the live listener is line 464.… | FIXED |
| P3 | `your-journey.html:484` | On a mismatch with the engine unread, fxPreview(true) disables #fxg (line 439 `g.disabled=!plan.ready`) and the very next statement re-enables it, so … | FIXED — the button follows the plan's readiness |
| P3 | `your-journey.html:328` | 'Leave the waiting list' ignores the unwait result: on an unreachable engine the card re-renders unchanged with no message, unlike every other engine … | FIXED — the refusal is said |
| P3 | `assets/rooms-data.js:246` | The Riverside comment (rooms-data.js:244-247) and the acceptance README (docs/acceptance/2026-09-19-release-014/README.md:37, decision 6) still report… | FIXED — the current master says 30 everywhere |
| P3 | `src/experience-inventory.json:433` | Inventory rows inconsistent with experiences.js: the two NEW places carry price:null / hours:null (Baan Phraya 433-434, Cannubi 721-722, both 'sheet':… | FIXED — the prices live in the hours text of the record; the inventory carries no separate price |
| P3 | `assets/experiences.js:45` | The Sühring record adds a sentence that is not in the master: practical.hours[3] "The hosts' evening is arranged with the house through Guest Relation… | FIXED — the record's hours stand verbatim; the dinner date is a `when` field |
| P3 | `assets/images/ASSET-MAP.md:68` | Stale records left in tracked documentation: ASSET-MAP.md:68 still lists 'Private Residence (alternative stay)' as the Vientiane alternative; docs/IMA… | FIXED (ASSET-MAP, IMAGE-ASSET-COVERAGE); PROJECT_MASTER_BRIEF.md is local-only and superseded by the Owner's instruction |
| P3 | `assets/pricing.js:350` | No migration exists for a bag or draft line saved under the retired ids 'airbnb-2br' / stay 'airbnb': repriceFlat (pricing.js:350) and repriceAccommod… | FIXED — see above |

Findings: 58 (P1 15 · P2 19 · P3 24). Refuter verdicts returned: 36 (refuted 29). Refuted-as-already-fixed included the XSS/name finding (the fix was in the tree before the refuters read it).

Every "FIXED" is covered by the unit suite (390 tests) and the stage E2E suites recorded under docs/acceptance/2026-09-19-release-014/.
