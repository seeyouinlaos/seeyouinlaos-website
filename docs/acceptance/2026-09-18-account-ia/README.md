# 010 — Account IA · My Profile (Owner correction, 18 Sep 2026)

Branch `account-ia-010` on top of main `c545d47` (release 009). Synthetic guests only; no real guest data touched.

## What changed
- **Sticky header** = MENU | see you in laos. | BAG ICON. Under it the account row **MY TRIP · MY PROFILE · SIGN OUT** (`assets/invite.mjs`). The textual My Bag is gone from the row: the bag icon IS My Bag (badge, always opens `cart.html`). The bottom Bag summary keeps the total · Open My Bag · Top ↑.
- **`profile.html` — My Profile, the account dashboard.** Authenticated (private routing: signed out → the invitation page with the way back), **outside the step shell**: it never loads `assets/prep-shell.js`, is not in `STEPS`, never redirects and never counts towards readiness. It reads: identity (photo/initials, preferred name, full name, host role · personal invitation, party), contact (email · mobile · Edit contact details → invitation.html#contact), trip status (Saved as draft / Changes saved · not yet sent / Sent to Guest Relations · Under review / Confirmed by Guest Relations, the reference, the sent time in words), the arrangements as framed 5:4 image cards (fixed arrangements from `SIYL_ARRANGED` — "Arranged for you · Fixed arrangement · not part of your bag", no amount; the held rooms — "Your place is held · Room B"; travel with the class; experiences; the wedding: temple morning · Vow Ceremony · Wedding Dinner with the seat "Seat E5 · Held in your name"; View details / Change in My Bag), tickets & passes (seat tickets and travel passes → `tickets.html`), About You (step 05 · Complete / n answers still needed · Edit About You → `about-you.html`), documents (Passport · Flight information · publication choice → `about-you.html#documents`), Sign out. No total of its own, no add/remove — My Bag stays the one cart.
- **Step 05 = About You** (`assets/prep-shell.js`, `assets/guest.js`, `about-you.html` title/h1/own line "05 / 06 · About You", the menu, the register validation words). Review & Send still requires it.
- **The step header, calm** (`assets/prep-shell.js`, `assets/prep.css`): no PRIVATE eyebrow; 1 the step and its title · 2 the guest and their party · 3 the state (sentence case, quiet, with the saved time on the same line) · 4 Save My Progress · 5 View all steps. 156 px at 320/390, 155 px from 834.
- **Profile photo** (`src/worker.js` `/api/profile/photo`, `assets/avatar.js`): the browser draws the picture on a canvas (centred square, ≤ 512 px, JPEG) so the original never leaves the device; the Worker stores it in the existing **REG_KV** namespace under `avatar:<invitationId>` with the guest's bearer only (`identify`), JPEG · PNG · WebP, ≤ 1 MB; GET returns the bytes to the owner only with `cache-control: private, no-store` (404 for anyone else — verified with a second synthetic guest); DELETE removes it; nothing is listed, nothing is public, nothing is written to localStorage or the repository. **No new binding is required** — the register store already holds contact and draft data per invitation with the same auth chain.

## Evidence
| File | What it is |
|---|---|
| `e2e.mjs` → `e2e.json` | the browser E2E on the isolated stage worker (31 checks): the nine Owner checks (profile with nothing complete → no redirect; profile at every readiness state; bag icon = My Bag; the sticky row MY TRIP · MY PROFILE · SIGN OUT; no textual My Bag; About You = step 05 and gates Review & Send; readiness untouched by the profile; held room / seats / fixed arrangement on the profile without a bag duplicate; Sign out) + the photo round trip (upload, private read, reload, another guest 404, remove, 415/413/401) + 320/834/1440 overflow checks |
| `shell-scroll.mjs` | the sticky-shell regression of release 009 with `profile.html` added: 264 checks · 0 failures at 320/390/834/1440 |
| `e2e-p0-rerun.json` | the P0 Empty Bag E2E re-run on this branch: PASS (49 checks) |
| `shots/` | 390 (primary acceptance): profile empty / complete / scrolled rail / photo / host with the fixed arrangement, My Trip header, About You; 320 · 834 · 1440 profile; `after-*` the recomposed step header and the profile rails at 320/390/834/1440 |
| `test/profile.test.mjs` (suite 323) | the nine checks as source pins + the photo route with a fake KV |

Codex adversarial review: **DEFERRED — usage limit until 13:41** (18 Sep 2026); the confirming pass runs at the next window together with the RELEASE-3 pass. Per the Owner's rule the work did not idle.
