# The final booking-UX pass (Owner instruction, 20 Sep 2026)

"Finish the website now": the guest stays inside the six-step flow, the wedding dinner shows who sits where, the seat is
confirmed before the guest continues, the photography words, Coffee & Cake, the profile photo made obvious, Save my
progress, the dress-code galleries, the Highlight dress codes, the train ticket, the Lijiang room — and every working
booking rule preserved. Production carries real guest activity: **nothing was reset**, no code, guest or identity changed.

## 1 · What changed

| # | Section | Change |
|---|---|---|
| 1–3 | **The booking context** | `assets/wizard.js` (new). A Journey page opened from 02 / 06 · My Trip (a stay, a room, a train, a flight, a Highlight table, the tea, the Marsilea) is a **sub-view** of that step: the context travels explicitly in the URL — `?ctx=trip&stage=<key>` — never guessed from a referrer, never kept in a store. My Trip rewrites the links it opens with the stage of the card they sit in; the sub-view mounts the step shell (02 / 06 · My Trip), its way back reads **RETURN TO MY TRIP** (`your-journey.html#s-<stage>`), links between the Journey's own pages keep the context, the global menu / footer / shell never carry it. A selection (`ST.select`, a unit pick, a transport add, a Highlight confirm, the tea) calls `SIYL_WIZARD.done(stage)` → `your-journey.html?done=<stage>#s-<stage>` after a breath: the stage is named ("Selected · in your trip · Next: <label>"), the next open stage linked right below (`.p-wizard-note`, `.p-wizard-landed`). Opened from the global menu the same page is the ordinary Journey page (section 18: two navigations, kept apart). |
| 4 | **Booking logic** | Untouched: the same engine, Bag, pricing, selection, waiting list (`ST.select` · `rooms.js` · `bag.js` `put` replaces by id · `pricing.js` · `journey.js`). Essential = D1 only, packages as configuration — as shipped in 014. The manual flow's total in the E2E (T003, Join All, the cheapest fares) is USD 2,057 with 10 lines; the Owner's own manual test (Business fares, other rooms) reached USD 3,919 — pricing unchanged. |
| 5 | **Temple Offering USD 15** | Investigated, **not changed**: `assets/temple.js` `sync()` writes the `sangkhathan` line (USD 15 per guest, "a personal offering, prepared for you and presented by you") only when the guest attends the temple AND answered **Yes** to "Would you like a Sangkhathan prepared in your name?" (eligibility from the register). Answering No removes the line. Opt-in, consistent with the approved rule. |
| 6 | **Special Express No. 25 ticket** | Root cause at the shared component: `.p-sum-ticket` had no top padding, so the ticket panel sat on the image edge (worst on desktop, where the ticket spans the grid). Fixed in `assets/prep.css` — `padding: var(--s4) var(--s5) var(--s5)`; desktop `var(--s5) var(--s6) var(--s6)`. Verified at 320 · 390 · 834 · 1440 (`stage/*-train-card.png`). |
| 7 | **Lijiang · Snow Mountain Viewing Room** | The Owner's Drive folders for Lijiang hold **no photograph of this room** (024 has 001–009 of other categories). Nothing invented: the one approved frame stays and is now captioned honestly — "The view from the room · Jade Dragon Snow Mountain over the Baisha rooftops (the room itself is not yet photographed)", `viewOnly: true` in `assets/rooms-data.js`. **Asset gap to the Owner.** |
| 8 | **WHO SITS WHERE** (must-have) | The seating ledger (`src/seating.js`) adds `holder` (the opaque guest id) to a held chair **for an authenticated read only** (`named: true`); the public read stays `named: false` — no name, no holder. `src/worker.js` `GET /api/profile/photo?of=<guest id>` lets a signed-in guest read another guest's portrait (401 without a bearer; 404 for a missing photo or another shape; `of` is never read on a write). `assets/avatar.js` `SIYL_AVATAR.of(id)` (cached per page, cleared on sign-out); `assets/seating.js` `railHtml` / `wireRail`: the roll call beside the plan — a small portrait (the photo where one exists, initials where not), the first name ("You" for the guest's own chair), the seat label; re-rendered with every seating change (the same view). On Wedding Preparation (beside the plan) and on My Profile (the dinner card). |
| 9 | **Profile photo discoverability** | My Profile: the portrait is a button (`.pf-avatar[data-photo-add]`, dashed ring + camera badge when empty), the CTA **ADD PROFILE PHOTO**, a note that the photo appears beside the guest's name in the seat plan. The uploader, isolation and storage are the existing ones (own bearer, `avatar:<invitation>`). |
| 10 | **CONFIRM SEAT first** | While a chair is chosen but unconfirmed the seat bar's **CONFIRM SEAT** is the primary action and the step's Continue steps back (`body.seatbar-on .prep-foot [data-continue] { opacity: .35; pointer-events: none }`); once held, Continue returns. |
| 11 | **Photography & Film** | About You reads exactly: "Photography and filming take place during the wedding day, and selected photographs and films may be published on our wedding website and social media." + the checkbox "I understand and acknowledge this." (`assets/guest.js` PHOTO_TEXT · PHOTO_VERSION 2026-09-20; earlier acknowledgements stay valid). |
| 12 | **Coffee & Cake** | Its own complimentary wedding item on My Profile (`data-profile-item="wedding:coffee"`): Sunday, 28 February 2027 · from 12:00 (the venue data's approved time), Souphattra Heritage, Joining / Not joining from the guest's own answer, cost Complimentary. |
| 13 | **My Profile design** | Preserved: the same card grammar; only the Coffee & Cake card, the photo action and the roll call added. |
| 14 | **Save My Progress** | The account block of the menu (`assets/invite.mjs`) gains **Save my progress** (`SIYL_DRAFT.flush('menu')`, the existing draft mechanism) with "Saved · HH:MM". |
| 15 | **Dress-code galleries** | `assets/refgal.js` (new): the reference rails on dress.html and Wedding Preparation become the website's one carousel (`SIYL_AMAN.wire`): 3:4 cards, one at a time with the next peeking on a phone, two on a tablet, three on a desktop, previous / next, the count, swipe, snap, keyboard. Every reference reachable (6 · 6 · 5), no trapped last image, no page overflow, at 320 · 390 · 834 · 1440. |
| 16 | **Highlight dress codes** | Baan Phraya ("Elegant attire") and Cannubi ("Smart casual") — from the houses' own practical information already in the data, shown on the Highlight page in the same block language (`experience.html [data-dress]`, a link to the dress-code page). Sühring names none → none invented. |
| 17 | **Responsive QA** | Element screenshots at 320 · 390 · 834 · 1440: the booking cards, the sub-views, the ticket panels, the galleries, the seat rail, the profile (`stage/`). No overflow, no page error. |
| 19 | **Browser history** | Room A → back → Room B → back / forward: one hold (the engine replaces by stage), one Bag line (`put` by id), the page repaints from the current Bag on `siyl:bag` — Room A never resurrected (E2E `history-room-a-then-b-one-hold`). |
| 20–22 | **No reset · go-live · Highlights preserved** | No production write; the register, codes, CONxxx / COUPLxxx untouched; the auth / profile work and the Highlights pass untouched (their suites re-run below). |

## 2 · Proofs

| Proof | Result |
|---|---|
| Unit (`npm test`) | **411 / 411** — `test/final-pass.test.mjs` (the context contract in a DOM sandbox · the ledger's holder + the portrait route through the Worker · the rail · CONFIRM SEAT · the words · Coffee & Cake · the photo · Save my progress · the galleries · the Highlight dress codes · the ticket · Lijiang) + the four re-pinned account-nav tests |
| Release gates | RELEASE CHECK PASSED (25 gates; V1 both clips; I1 infra freeze intact; `src/auth.js` untouched) |
| Stage E2E `e2e.mjs` → `stage/` | **36 / 36** — the manual flow through all ten stages as sub-views (Bangkok via the room page · the train on My Trip · Vientiane rooms · the pre-wedding pick · the flight · Kunming · Lijiang · Kempinski · C86 · the return), every excursion returning by itself with the stage named, 6 holds · 10 lines · USD 2,057 · Continue to The Wedding; browser history A → B; CONFIRM SEAT primary; the photography words; Review sendable; Coffee & Cake + the photo CTA + the roll call on My Profile; Save my progress; WHO SITS WHERE (Ada uploads, confirms; Cleo sees name · photo · seat; Ada moves; Cleo reloads; Ben shows initials; signed-out no names, `?of=` → 401); the galleries at four widths; the Highlight dress codes; the global Journey unchanged; the ticket at four widths; console clean |
| Regression suites Highlights · 014 · 013 · 012 · 011 · P0 · IA | **20/20 · 79/79 · 35/35 · 25/25 · 52/52 · 52/52 · 35/35** (`stage/regression.txt`) |
| Codex | **PENDING — EXTERNAL QUOTA LIMIT** (probed 20 Sep: "try again at Sep 24th, 2026 10:19 PM"); not represented as passed; the questions recorded in `docs/review/015-codex-release-014/PLAN.md` |
| Deploy · parity · live-ro | main `a9baea7` → Workers Build version **`cf22b228`** (15:30:10Z, 20 Sep 2026) · parity **259 / 259** (`live/parity.json`) · infra freeze intact (`infra-guard --live`) · the deployed register identical to the repository (`register-audit`: 104 invitations · 104 unique codes · 0 missing · 0 duplicate · 0 auth errors) · live-ro **25 / 25** (`live/live-ro.json`) · production read-only smoke signed out at 390 and 1440 (`live/live-*.jpg`): the galleries 6 · 6 · 5, the Highlight dress codes, a sub-view URL signed out hands over to the invitation with its context in `next`, the global Journey with no step shell, no overflow, no page error; `GET /api/profile/photo?of=…` without a bearer → 401; the public `/api/seating` `named:false` with no holder and no name |
| Production transactional state | read-only dry runs before (15:27Z) and after (15:33Z) the deploy, aggregates only (`live/zero-state-cf22b228.txt`): **12 room occupancies · 0 waitlisted · 1 seat hold · 5 drafts of 104 actors · 13 KV keys — identical**; the value digest moved between the two reads (a live guest's draft re-saved — real activity), the counts did not; **nothing reset**, no execute call was ever made |
