# Email CI redesign (Owner, 16 Sep 2026) — live at a3464c5

Presentation only: `src/mail-templates.js` composes both emails from the stored record; `sendMail` passes `htmlContent` + `textContent` to Brevo. Recipients, ids, persistence, retry and the Brevo configuration are untouched.

- Guest email: wordmark · YOUR JOURNEY · rule; "Your journey has been received" / "has been updated"; Dear <first name>; Reference + Sent/Updated in words (Berlin time, never ISO); TRAVEL · STAYS (the engine's room) · EXPERIENCES · WEDDING (programme times and places) · YOUR SEATS (Seat E5 / Seat A5, hosts front centre) · ABOUT YOU · YOUR COST; OPEN YOUR JOURNEY → the Worker; codes never sent; Guest Relations; footer. No system word, no internal id, no version text; optional documents not provided are omitted.
- Guest Relations email: "New journey received" / "Journey updated" (latest version time, first sent); guest · party · email · mobile · reference · status; the same sections plus DOCUMENTS with "Still needed"; COST; INTERNAL REFERENCE last (guest, invitation, seat records, submission version, status URL).
- Tests: `test/mail-ci.test.mjs` (seat mapping pinned against `assets/seatlabels.js` for all 100 seats, the debug-word scan, no raw stamp, no id for the guest, CI markers); suite 291/291.
- Live test (`live-test.json`): the controlled guest G002 with a full journey (train, U Sathorn Room C from the engine, seats E5/A5, wedding answers, About You, USD 292, a passport not provided) → Guest Relations and guest accepted by Brevo with message ids; the guest email arrived in the test inbox as the CI HTML. Screenshots (the delivered HTML rendered at 390 px and 800 px): `guest-gmail-mobile.png`, `guest-relations-mobile.png`, `guest-desktop.png` — no horizontal scroll. Afterwards the room, the seats and the G002 records were removed.
