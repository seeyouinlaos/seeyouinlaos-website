# 002 — FINAL TARGETED CORRECTION PASS (Owner screenshots, 14 Sep 2026)

Seven iPad screenshots, one stream, verified against the live Worker / Pages and the source first.

| Item | Root cause | Correction |
|---|---|---|
| **Review & Send** — after SEND the guest could not tell whether the journey went | the success path only opened the "What happens now" box; the button kept reading SEND TO GUEST RELATIONS | the send area now says what the server proved: a **RECEIVED** card ("Your journey has been received by Guest Relations · Received · date · time · your time") painted only from the Worker's 202 (the record stored in REG_KV, the stamp echoed), the button becomes **✓ SENT TO GUEST RELATIONS** (aria-disabled, a second tap sends nothing), focus lands on the card; the state is derived again on every reload from the server's status + this device's send snapshot — changed since → "Send to Guest Relations again"; a newer version from another device → the send offered, SENT not claimed; failure → the backup channel, no success; CONFIRMED untouched. The Worker's e-mail attempt (`mailed`) is never claimed to the guest |
| **Bride & Groom inventory** — INV-001 could not select the Presidential (both windows), the Solarium Bath Suite, the 270° Snow Mountain View Suite | genuine eligibility defect, not a stale session: `room.reserved` was a label enforced for everyone in the browser, and the ledger's `sellable()` subtracted every held unit for every party | one rule in both places: the calculation source (`SIYL_PRICE.hosts()` = the bundle's explicit `hosts: true`; `reservedFor()` / `eligible()`), and the ledger (`HOSTS_INVITATION = 'INV-001'`, `heldForParty()`, `sellable(key, invitationId)`; `GET /api/inventory?invitation=` reads the ledger as that party; reserve / release / conflicts likewise). Family stays out of reach of everyone. Labels stay ("Reserved for bride & groom · held for your party — yours to choose"). Presets still choose from open rooms only |
| **Siam Kempinski** — "missing Add to your journey" while the row said SELECTED | the stay cards on journeys.html had no action of their own; the only path was the row → room page | one state model under every stay: **Selected · in your journey · ‹room›** with CHANGE ROOM (only where more than one room can be chosen — the rows become the choice in place, "Keep this room" cancels) and REMOVE FROM YOUR JOURNEY; open stays get ADD TO YOUR JOURNEY (one room → added at once; several → the rows become the choice). Never a second ADD for a chosen room; the room page keeps Add / Remove / Change selection |
| **Special Express No. 25** USD 75 | Owner value USD 100 | `assets/pricing.js` FLAT.train 100 (the one source), the journeys card, the retired engine's data + gate P4 + tests; every total that carried the train moved by 25 (Full Experience canonical 2,130 → **2,155**) |

Coverage: `test/eligibility.test.mjs` (catalogue · host / normal / family parties in the browser source · the ledger's sellable and reserve / read / release per party · surfaces); `test/pricing.test.mjs`, `test/inventory.test.mjs`, `test/registration.test.mjs`, `test/guest-testing.test.mjs`, `test/efg.test.mjs` pins moved; `walk.mjs` **31/31** (mocked API with the ledger's rules); `live-check.mjs` on production after deployment.

## Release and live verification (14 Sep 2026)
- **`21d2f9f`** on `main` — automatic Cloudflare Workers Build + Pages; parity **259/259** (local = Worker = Pages).
- Local before the push: `npm test` 244/244 (`test/eligibility.test.mjs` new) · gates PASS (P4 now pins the train at USD 100) · `walk.mjs` 31/31 · release walk 85/85 · guest-testing 52/52 · E/F/G 37/37 · a11y 18/18 · steps-nav 50/50 · seating UX 154/154.
- **`live-check.mjs` on production — 20/20** (`live/live-check-results.json`, frames): the ledger read as INV-001 opens all four Bride & Groom categories (Presidential ×2 windows, Solarium, 270° suite), as INV-002 they stay reserved; INV-002 asking for the Presidential is refused 409 "Bride & Groom" (nothing written); INV-001 reserving its own categories is accepted, then its allocation put back exactly (`wedstay/heritage` · 1 unit · qty 2); a **fresh INV-001 login** (signed out, session cleared, code entered again) sees every one of the four rooms as "Reserved for bride & groom · held for your party — yours to choose" with ADD TO YOUR JOURNEY and the live count, selects them, journeys rows open; INV-002 sees the label and no button; Special Express No. 25 USD 100; a **real Review & Send by INV-002** against the real register endpoint → RECEIVED card with the server's stamp, ✓ SENT TO GUEST RELATIONS, kept across a reload; then the test record `reg:INV-002` deleted from REG_KV and the test allocation released — INV-002 back to no record / no allocation, INV-001's record (received 13:54 UTC) and allocation lines untouched.
- `walk.mjs` 31/31 on the Worker and on Pages (mocked API) · smoke 8/8 both.
- Owner seat holds untouched by this pass (INV-002 ceremony E2/F2 + dinner B13/B14 as found at the end; INV-001 dinner A13/A14).
