# Final live quickfix — the guest booking flow (Owner, 16 Sep 2026)

Live on the Worker at **04a7653** (the flow) and **7afa36f** (the send-button repaint), `https://seeyouinlaos-website.suthep-hrg.workers.dev`. Suite 285/285, release check passed.

## What changed
- **One server-side journey draft per guest** — `/api/draft` (GET/PUT, the guest's own invitation only): the complete journey keys (contact, About You, bag/cart with the rooms, wedding attendance, documents state, sent stamp, skipped stages) merged on the server, never emptied by a device that holds less. `assets/draft.js`: autosave on every change and on Continue, the server copy pulled on sign-in (a never-synced device reads before it writes), **SAVE MY PROGRESS** on every private step (`SAVING…` → read-back → `SAVED · time`, or `SAVE FAILED · TRY AGAIN`). Rooms and seats live in their engines already.
- **One logical journey** — the first Review & Send sets the reference; every later send is an update of the same submission (version + 1, "Journey updated" to Guest Relations and the guest, previous version kept). `CHANGES SAVED · NOT YET SENT TO GUEST RELATIONS` is a server comparison of the sent fingerprint with the saved draft + engines, with **SEND UPDATED JOURNEY**. Success screen: ✓ Journey saved · ✓ Sent to Guest Relations · ✓ Confirmation email sent to <masked> · Reference · Sent time (or ! Confirmation email could not be sent + Retry, never a new submission).
- **Guest Relations view** — `/api/gr/journeys` (GR token): guest, status, reference, version, times, contact, bag, wedding, About You, rooms (engine), seats (engine), mail status.
- **Inventory from the current Master** (re-read 17:17 UTC): U Sathorn 6 rooms, Shama 6 rooms (38 / 27 retired everywhere); one canonical availability object per category in the engine (`sourceRooms/Places · ownerReserved · guestOccupied · remainingRooms/Places · soldOut = remainingPlaces === 0`) rendered directly by every surface. **Root cause of "38 rooms · 76 places available + FULLY BOOKED"**: the CTA derived "Fully booked" from *this guest may not choose here* (`fits`), which is false for a host whose Bangkok room is fixed — not from the inventory. Now "Fully booked" comes only from `soldOut`; a guest who may not choose reads "Reserved" or "Your room is fixed". REMOVE releases exactly the guest's allocation and only removes the bag line the engine released.

## Live evidence (fresh contexts, controlled guest G002, records removed afterwards)
- `cross-device.json`, `device-a-saved.png`, `device-b-synced.png` — device A: contact, About You, train, U Sathorn Room B, wedding answers, dress, seats, SAVE MY PROGRESS → SAVED; hard refresh keeps all; device B (fresh browser, same code): same contact, answers, bag, wedding, steps, room (engine), seats (engine); a change on B → A sees it. **CROSS-DEVICE: PASS**.
- `submit-update-remove.json`, `sent.png`, `changes-not-sent.png`, `updated.png`, `review-changes-not-sent.png` — Review & Send: SYL-G002-34DBEFD3, both emails accepted by Brevo (ids in the JSON), both arrived in the test inbox; room C → CHANGES SAVED · NOT YET SENT; Send updated journey → same reference, version 2, "Your Journey has been updated" accepted and arrived; REMOVE ×3: 11 → 12 places each time, never sold out, "6 rooms · 12 places available · Select this stay".
- `soldout.json` — the 2-place fixture on the live engine: A, B hold, C refused 409, burst refused, Room B 404, released, plan restored.
- `reconciliation.json/.txt` — all 40 categories against the current Master: ALL ASSERTIONS HOLD (U Sathorn/Shama 6 rooms · 12 places, Penthouse A–F with A the hosts' 2/2, no invented room anywhere).
- Haruthai's stored submission SYL-G048-2745CBB7: guest confirmation retried through the corrected recipient path — accepted by Brevo, message id recorded on the record; no new submission, selections untouched.
- Cleanup: G002's room and seats released, `draft:/contact:/reg:` test keys deleted; the rooms plan and the seating equal the snapshot taken before (only the hosts' genuine data remains).
