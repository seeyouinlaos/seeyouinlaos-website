{
  "name": "seeyouinlaos-flight-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "See You In Laos wedding website + Flight Tracker Travel Service (Cloudflare Worker).",
  "main": "src/worker.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
